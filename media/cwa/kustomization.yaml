apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

helmGlobals:
  chartHome: ../../charts

helmCharts:
- name: base-app
  releaseName: cwa
  valuesFile: values.yaml

resources:
- external-secret.yaml
