global:
  tolerations:
  - key: "role"
    operator: "Equal"
    value: "cp"
    effect: "NoSchedule"

preUpgradeChecker:
  jobEnabled: false

persistence:
  defaultClassReplicaCount: 2

defaultBackupStore:
  backupTarget: "nfs://nas.homelab:/volume1/${INFRA_ENV}/backup/longhorn"
