apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: slskd
  namespace: p2p
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  rules:
  - host: slskd.${INFRA_DOMAIN}
    http:
      paths:
      - backend:
          service:
            name: slskd
            port:
              name: web
        path: /
        pathType: Prefix
