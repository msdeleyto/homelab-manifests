apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: slskd-route
  namespace: p2p
spec:
  parentRefs:
  - group: gateway.networking.k8s.io	
    kind: Gateway
    name: cluster-gateway
    namespace: network
    sectionName: https
  hostnames:
  - "slskd.${INFRA_DOMAIN}"
  rules:
  - backendRefs:
    - name: slskd
      port: 5030
