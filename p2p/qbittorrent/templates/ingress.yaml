apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qbittorrent
  namespace: p2p
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  rules:
  - host: qb.${INFRA_DOMAIN}
    http:
      paths:
      - backend:
          service:
            name: qbittorrent
            port:
              name: web
        path: /
        pathType: Prefix
