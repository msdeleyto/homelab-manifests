apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: qb-route
  namespace: p2p
spec:
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: cluster-gateway
    namespace: network
    sectionName: https
  hostnames:
  - "qb.${INFRA_DOMAIN}"
  rules:
  - backendRefs:
    - group: ''
      kind: Service
      name: qbittorrent
      port: 8080
    matches:
    - path:
        type: PathPrefix
        value: /
