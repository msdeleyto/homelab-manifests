nameOverride: shelfmark
fullnameOverride: shelfmark

image:
  repository: ghcr.io/calibrain/shelfmark
  tag: v1.3.11@sha256:394b703c5409a8669af246179d8e5ec23b971565b35000c9ac1e150454d66e1c

serviceAccount:
  create: false

service:
  type: ClusterIP
  ports:
  - name: http
    port: 8084
    targetPort: http
    protocol: TCP

ports:
- name: http
  containerPort: 8084
  protocol: TCP

httpRoute:
  enabled: true
  parentRefs:
  - name: cluster-gateway
    group: gateway.networking.k8s.io
    kind: Gateway
    sectionName: https
    namespace: network
  hostnames:
  - "shelfmark.${INFRA_DOMAIN}"
  port: 8084
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /

livenessProbe:
  httpGet:
    path: /api/health
    port: http

storage:
  create: true
  name: shelfmark-config
  className: longhorn
  accessModes:
  - ReadWriteOnce
  size: 1Gi

env:
- name: PUID
  value: "1000"
- name: PGID
  value: "1000"
- name: DOCKERMODE
  value: "true"

volumes:
- name: config
  persistentVolumeClaim:
    claimName: shelfmark-config
- name: books
  nfs:
    server: nas.homelab
    path: /volume1/data/media/books/new

volumeMounts:
- name: config
  mountPath: /config
- name: books
  mountPath: /books

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
      - matchExpressions:
        - key: node-role.kubernetes.io/control-plane
          operator: NotIn
          values:
          - "true"
